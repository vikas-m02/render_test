services:
  - type: web
    name: webhook-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn webhook_listener:app
    envVars:
      - key: PORT
        value: 10000
